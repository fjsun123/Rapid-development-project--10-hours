# 后台管理端 - 手工测试用例

> 范围：后台管理 Web，含 5 个业务页面 + UI 组件库 + 公共导航布局 + 全局兜底，共 69 条用例
> 测试人员：____
> 测试环境：参见 [00-测试用例总览.md](./00-测试用例总览.md) §2
> 浏览器：Chrome 120+ / Safari 17+，分辨率 ≥ 1280×800
> 测试账号：admin / admin123（参见总览 §3）

---

## 用例分布

| 模块 | P0 | P1 | P2 | 小计 |
|---|---|---|---|---|
| 公共导航与布局 | 1 | 1 | 2 | 4 |
| 首页 (/dashboard) | 2 | 3 | 3 | 8 |
| 订单管理 (/orders) | 6 | 6 | 2 | 14 |
| CRM (/crm) | 2 | 5 | 2 | 9 |
| 菜单管理 (/menu) | 5 | 6 | 2 | 13 |
| 设置 (/settings) | 4 | 3 | 2 | 9 |
| UI 组件库 (/ui-library) | 0 | 4 | 4 | 8 |
| 错误兜底与全局行为 | 1 | 2 | 1 | 4 |
| **合计** | **21** | **30** | **18** | **69** |

---

## 1. 公共导航与布局

### TC-A-001 [P0] 未登录访问受保护页面应重定向到登录页

| 字段 | 内容 |
|---|---|
| 模块 | 公共导航与布局 |
| 前置条件 | 浏览器已清除登录态（清空 localStorage / cookies） |
| 测试数据 | 直接访问 `/orders`、`/menu` 等受保护路由 |
| 优先级 | P0 |

**测试步骤**：
1. 打开 Chrome 隐身窗口，访问 `http://localhost:3000/orders`
2. 观察页面跳转
3. 使用 `admin / admin123` 登录
4. 观察登录后的跳转

**期望结果**：
- ✅ 界面：步骤 2 自动跳转至 `/login`，URL 携带 `?redirect=/orders` 或类似参数
- ✅ 界面：步骤 4 登录成功后跳转回 `/orders`
- ✅ 数据：localStorage 写入 token

**通过 / 失败**：☐ / ☐  执行人：____  执行时间：____  Bug 编号（如有）：____

---

### TC-A-002 [P1] 侧边栏菜单项切换并高亮当前路由

| 字段 | 内容 |
|---|---|
| 模块 | 公共导航与布局 |
| 前置条件 | 已登录管理员 |
| 测试数据 | 5 个菜单项：首页、订单、CRM、菜单、设置 |
| 优先级 | P1 |

**测试步骤**：
1. 登录后默认进入首页，观察「首页」菜单项是否高亮
2. 依次点击「订单」「CRM」「菜单」「设置」
3. 每次点击后观察 URL 与高亮项

**期望结果**：
- ✅ 界面：左侧侧边栏固定宽度 256px
- ✅ 界面：每次点击后对应菜单项背景色高亮（使用 color-primary 相关令牌）
- ✅ 数据：URL 分别变为 `/`、`/orders`、`/crm`、`/menu`、`/settings`
- ✅ 界面：UI 库 (`/ui-library`) 不在主菜单中显示

**通过 / 失败**：☐ / ☐  执行人：____  执行时间：____  Bug 编号（如有）：____

---

### TC-A-003 [P2] 退出登录清除登录态

| 字段 | 内容 |
|---|---|
| 模块 | 公共导航与布局 |
| 前置条件 | 已登录管理员 |
| 测试数据 | 无 |
| 优先级 | P2 |

**测试步骤**：
1. 在右上角找到用户头像 / 退出按钮
2. 点击「退出登录」
3. 观察页面跳转和 localStorage

**期望结果**：
- ✅ 界面：跳转回 `/login` 页面
- ✅ 数据：localStorage 中 token 被清除
- ✅ 后退按钮不能回到受保护页面（再次跳走）

**通过 / 失败**：☐ / ☐  执行人：____  执行时间：____  Bug 编号（如有）：____

---

### TC-A-004 [P2] 全局布局设计令牌一致性巡检

| 字段 | 内容 |
|---|---|
| 模块 | 公共导航与布局 |
| 前置条件 | 已登录，打开 Chrome DevTools |
| 测试数据 | 主色 #F59E0B、背景 #F9FAFB、卡片 #FFFFFF、边框 #E5E7EB |
| 优先级 | P2 |

**测试步骤**：
1. 在任一业务页面，用 DevTools 选中主要按钮，检查 background-color
2. 选中右侧主内容区，检查 padding 是否为 24px (spacing-lg)
3. 选中页面背景，检查色值

**期望结果**：
- ✅ 界面：主要按钮背景为 #F59E0B
- ✅ 界面：内容区 padding = 24px
- ✅ 界面：页面底色 #F9FAFB，卡片白底 #FFFFFF
- ✅ 界面：边框色统一 #E5E7EB

**通过 / 失败**：☐ / ☐  执行人：____  执行时间：____  Bug 编号（如有）：____

---

## 2. 首页 (/dashboard)

### TC-A-005 [P0] 首次进入首页加载 KPI 卡片与最近订单

| 字段 | 内容 |
|---|---|
| 模块 | 首页 |
| 前置条件 | 已登录管理员；数据库存在至少 1 笔订单 |
| 测试数据 | seed 中的订单数据 |
| 优先级 | P0 |

**测试步骤**：
1. 打开 Network 面板，访问 `/` 或 `/dashboard`
2. 观察加载过程中的骨架屏
3. 等待数据返回，观察 KPI 卡片和最近订单表格

**期望结果**：
- ✅ 界面：加载期间显示 4 个 KPI 卡片骨架 + 5 行表格骨架
- ✅ 界面：渲染 4 张卡片（今日订单数、今日收入、待处理订单、热门商品），高度 120px
- ✅ 界面：最近订单表格最多 5 行
- ✅ 接口：并行请求 `GET /api/dashboard/stats`（200）+ `GET /api/orders?page=1&size=5`（200）

**通过 / 失败**：☐ / ☐  执行人：____  执行时间：____  Bug 编号（如有）：____

---

### TC-A-006 [P0] 点击待处理订单卡片跳转订单页并自动筛选

| 字段 | 内容 |
|---|---|
| 模块 | 首页 |
| 前置条件 | 已登录；存在 ≥ 1 笔 pending 订单 |
| 测试数据 | seed 数据 |
| 优先级 | P0 |

**测试步骤**：
1. 在首页找到「待处理订单」卡片
2. 点击卡片数值
3. 观察跳转后的订单页

**期望结果**：
- ✅ 界面：跳转至 `/orders`，订单状态筛选下拉自动选中「待接单」
- ✅ 界面：表格只显示 pending 状态的订单
- ✅ 接口：`GET /api/orders?status=pending&page=1&size=50` 200

**通过 / 失败**：☐ / ☐  执行人：____  执行时间：____  Bug 编号（如有）：____

---

### TC-A-007 [P1] 首页最近订单为空时显示空态文案

| 字段 | 内容 |
|---|---|
| 模块 | 首页 |
| 前置条件 | 已登录；清空 orders 表（仅测试环境，可用 SQL：`DELETE FROM order_items; DELETE FROM orders;`） |
| 测试数据 | 无订单数据 |
| 优先级 | P1 |

**测试步骤**：
1. 清空订单数据后刷新首页
2. 观察最近订单区块

**期望结果**：
- ✅ 界面：表格区域显示「暂无订单，去创建第一单」
- ✅ 界面：KPI 卡片今日订单数 = 0，今日收入 = ¥0.00

**通过 / 失败**：☐ / ☐  执行人：____  执行时间：____  Bug 编号（如有）：____

---

### TC-A-008 [P1] 点击最近订单行打开详情抽屉

| 字段 | 内容 |
|---|---|
| 模块 | 首页 |
| 前置条件 | 已登录；最近订单列表至少 1 行 |
| 测试数据 | 任一订单 |
| 优先级 | P1 |

**测试步骤**：
1. 在首页最近订单表格点击任一行
2. 观察右侧滑出抽屉
3. 观察抽屉内容

**期望结果**：
- ✅ 界面：从右侧滑出宽度 560px 的抽屉
- ✅ 界面：抽屉显示订单基本信息、菜品明细、状态操作区
- ✅ 接口：`GET /api/orders/:id` 200 + `GET /api/orders/:id/available-status` 200

**通过 / 失败**：☐ / ☐  执行人：____  执行时间：____  Bug 编号（如有）：____

---

### TC-A-009 [P1] 点击「查看全部」跳转订单页（无筛选）

| 字段 | 内容 |
|---|---|
| 模块 | 首页 |
| 前置条件 | 已登录 |
| 测试数据 | 无 |
| 优先级 | P1 |

**测试步骤**：
1. 在首页最近订单区块右上角点击「查看全部」
2. 观察跳转

**期望结果**：
- ✅ 界面：跳转至 `/orders`，状态筛选为空，显示所有订单
- ✅ 接口：`GET /api/orders?page=1&size=50` 200

**通过 / 失败**：☐ / ☐  执行人：____  执行时间：____  Bug 编号（如有）：____

---

### TC-A-010 [P2] 加载失败时显示错误兜底与重试

| 字段 | 内容 |
|---|---|
| 模块 | 首页 |
| 前置条件 | 已登录 |
| 测试数据 | 在 DevTools Network 面板用 Block request URL 拦截 `/api/dashboard/stats` |
| 优先级 | P2 |

**测试步骤**：
1. DevTools Network 拦截 `/api/dashboard/stats`
2. 刷新首页
3. 观察错误状态
4. 取消拦截，点击「重试」按钮

**期望结果**：
- ✅ 界面：显示「加载失败，点击重试」文案 + 重试按钮
- ✅ 界面：步骤 4 后重新请求，加载成功后正常显示

**通过 / 失败**：☐ / ☐  执行人：____  执行时间：____  Bug 编号（如有）：____

---

### TC-A-011 [P2] KPI 卡片金额格式校验

| 字段 | 内容 |
|---|---|
| 模块 | 首页 |
| 前置条件 | 已登录；今日营收 > 1000 元 |
| 测试数据 | 制造一笔总额 12345 元的订单（可直接 SQL 插入或多次下单累计） |
| 优先级 | P2 |

**测试步骤**：
1. 进入首页
2. 检查「今日收入」卡片的金额展示

**期望结果**：
- ✅ 界面：金额格式为 `¥12,345.00`（千位分隔、保留 2 位小数）
- ✅ 数据：分转元处理正确（如后端返回 1234500 分，前端显示 ¥12,345.00）

**通过 / 失败**：☐ / ☐  执行人：____  执行时间：____  Bug 编号（如有）：____

---

### TC-A-012 [P2] 热门商品卡片显示销量

| 字段 | 内容 |
|---|---|
| 模块 | 首页 |
| 前置条件 | 已登录；今日存在已售出菜品 |
| 测试数据 | seed 订单 |
| 优先级 | P2 |

**测试步骤**：
1. 进入首页
2. 观察「热门商品」卡片

**期望结果**：
- ✅ 界面：显示商品名称 + 「今日售出 X 份」
- ✅ 界面：若今日无销售，显示「-」或合理占位

**通过 / 失败**：☐ / ☐  执行人：____  执行时间：____  Bug 编号（如有）：____

---

## 3. 订单管理 (/orders)

### TC-A-013 [P0] 创建包含 2 个菜品的订单且金额服务端计算

| 字段 | 内容 |
|---|---|
| 模块 | 订单管理 |
| 前置条件 | 已登录管理员；客户「张三」存在；菜品「宫保鸡丁 ¥38」「可乐 ¥10」可用 |
| 测试数据 | 客户「张三」；宫保鸡丁 x 2、可乐 x 1 |
| 优先级 | P0 |

**测试步骤**：
1. 进入 `/orders`，点击右上角「创建订单」
2. 步骤 1 在客户下拉搜索「张三」并选中，点「下一步」
3. 步骤 2 选「主食」Tab，宫保鸡丁数量改为 2；切「饮品」Tab，可乐数量改为 1
4. 观察底部合计金额
5. 点「下一步」进入步骤 3
6. 备注留空，点「提交订单」
7. 观察成功吐司与列表刷新

**期望结果**：
- ✅ 界面：步骤 2 底部合计显示 `¥86.00`（38*2 + 10）
- ✅ 界面：步骤 6 后吐司「订单创建成功」，模态框关闭
- ✅ 数据：orders 表新增一行，total_amount = 8600（分），客户 ID = 张三
- ✅ 数据：order_items 表新增 2 行，对应宫保鸡丁 qty=2、可乐 qty=1，单价快照来自菜品当前价
- ✅ 接口：`POST /api/orders` 返回 201 + 完整订单对象
- ✅ 数据：若设置中 autoAccept=true，订单状态为 confirmed；否则 pending

**异常分支**：
- 步骤 3 不选任何菜品直接进步骤 3 → 「下一步」按钮禁用或吐司「请至少选择 1 个菜品」

**通过 / 失败**：☐ / ☐  执行人：____  执行时间：____  Bug 编号（如有）：____

---

### TC-A-014 [P0] 创建订单时选择已下架菜品被拒

| 字段 | 内容 |
|---|---|
| 模块 | 订单管理 |
| 前置条件 | 已登录；存在 1 个已下架菜品（seed 中「测试已下架菜品」） |
| 测试数据 | 已下架菜品 |
| 优先级 | P0 |

**测试步骤**：
1. 点击「创建订单」，选客户「张三」
2. 步骤 2 观察「测试已下架菜品」是否出现在列表中
3. 若前端已过滤，则用 DevTools 修改请求体强制带入下架菜品 ID 提交

**期望结果**：
- ✅ 界面：菜品列表（`GET /api/menu-items?available=true`）不包含下架菜品
- ✅ 接口：强制提交时返回 422，错误码 `INVALID_ORDER_ITEMS`
- ✅ 界面：吐司「部分菜品已下架或库存不足」

**通过 / 失败**：☐ / ☐  执行人：____  执行时间：____  Bug 编号（如有）：____

---

### TC-A-015 [P0] 订单状态机正向流转 pending → confirmed → ready → completed

| 字段 | 内容 |
|---|---|
| 模块 | 订单管理 |
| 前置条件 | 已登录；存在一笔 pending 订单（设置中关闭自动接单后再创建） |
| 测试数据 | 任一 pending 订单 |
| 优先级 | P0 |

**测试步骤**：
1. 列表中点击该订单打开详情抽屉
2. 在抽屉「状态操作区」点「接单」按钮，观察状态徽章
3. 点「出餐」按钮
4. 点「完成」按钮
5. 每一步观察吐司、抽屉与列表

**期望结果**：
- ✅ 界面：每一步状态徽章依次变为「制作中（蓝）」「待出餐（绿）」「已完成（灰）」
- ✅ 界面：每步吐司「订单状态已更新」
- ✅ 接口：3 次 `PATCH /api/orders/:id/status`，请求体分别 `{status:"confirmed"}` / `{status:"ready"}` / `{status:"completed"}`，均 200
- ✅ 界面：completed 状态下，操作区不显示其他可执行按钮（available-status 返回空数组）

**通过 / 失败**：☐ / ☐  执行人：____  执行时间：____  Bug 编号（如有）：____

---

### TC-A-016 [P0] 非法状态转换被后端拒绝（completed 不能再变更）

| 字段 | 内容 |
|---|---|
| 模块 | 订单管理 |
| 前置条件 | 已登录；存在一笔 completed 订单 |
| 测试数据 | 任一 completed 订单 |
| 优先级 | P0 |

**测试步骤**：
1. 打开 completed 订单详情抽屉
2. 观察可执行操作按钮
3. （进阶）用 DevTools 直接 fetch `PATCH /api/orders/:id/status` 请求体 `{status:"cancelled"}`

**期望结果**：
- ✅ 界面：操作区不显示任何状态按钮
- ✅ 接口：手动调用返回 400，错误码 `INVALID_STATUS_TRANSITION`，msg「无法执行该状态转换」
- ✅ 界面：吐司错误信息

**通过 / 失败**：☐ / ☐  执行人：____  执行时间：____  Bug 编号（如有）：____

---

### TC-A-017 [P0] 取消订单二次确认且归还库存

| 字段 | 内容 |
|---|---|
| 模块 | 订单管理 |
| 前置条件 | 已登录；先记录某菜品库存（如「炒饭」库存=30），创建一笔包含该菜品 x 2 的 pending 订单 |
| 测试数据 | 包含「炒饭 x 2」的 pending 订单 |
| 优先级 | P0 |

**测试步骤**：
1. 创建订单后查菜单页确认「炒饭」库存=28
2. 进订单详情抽屉，点「取消」按钮
3. 在弹出对话框观察文案，点「再想想」关闭
4. 再点「取消」，弹窗后点「确定取消」（红色按钮）
5. 检查菜单页该菜品库存

**期望结果**：
- ✅ 界面：步骤 1 库存=28
- ✅ 界面：弹窗文案「确定取消该订单吗？取消后不可恢复，已扣减库存将归还。」
- ✅ 界面：步骤 3「再想想」关闭弹窗，订单状态不变
- ✅ 界面：步骤 4 确认后吐司「订单状态已更新」，订单状态变为「已取消（红）」
- ✅ 接口：`PATCH /api/orders/:id/status` body `{status:"cancelled"}` 200
- ✅ 数据：「炒饭」库存恢复为 30

**通过 / 失败**：☐ / ☐  执行人：____  执行时间：____  Bug 编号（如有）：____

---

### TC-A-018 [P0] 创建订单时金额服务端计算（前端篡改无效）

| 字段 | 内容 |
|---|---|
| 模块 | 订单管理 |
| 前置条件 | 已登录；DevTools 可拦截请求 |
| 测试数据 | 客户张三 + 宫保鸡丁 x 1（实际应为 ¥38） |
| 优先级 | P0 |

**测试步骤**：
1. 创建订单走到步骤 3
2. DevTools 拦截 POST 请求，修改请求体 `totalAmount` 为 1
3. 提交并观察接口响应中的 totalAmount

**期望结果**：
- ✅ 接口：响应体 totalAmount = 3800（分），等于服务端按 unitPrice × qty 重新计算的结果
- ✅ 数据：DB 存的 total_amount = 3800，与请求体传的 1 无关

**通过 / 失败**：☐ / ☐  执行人：____  执行时间：____  Bug 编号（如有）：____

---

### TC-A-019 [P1] 多状态筛选订单列表

| 字段 | 内容 |
|---|---|
| 模块 | 订单管理 |
| 前置条件 | 已登录；存在 pending 与 confirmed 各 ≥ 1 笔 |
| 测试数据 | seed 订单 |
| 优先级 | P1 |

**测试步骤**：
1. 在状态下拉同时选「待接单」「制作中」
2. 观察 URL 和表格

**期望结果**：
- ✅ 接口：`GET /api/orders?status=pending,confirmed&page=1&size=50` 200
- ✅ 界面：表格只显示这两种状态的订单
- ✅ 界面：取消选择后恢复显示所有

**通过 / 失败**：☐ / ☐  执行人：____  执行时间：____  Bug 编号（如有）：____

---

### TC-A-020 [P1] 日期范围「今天 / 本周」快捷筛选

| 字段 | 内容 |
|---|---|
| 模块 | 订单管理 |
| 前置条件 | 已登录；存在跨日订单 |
| 测试数据 | seed 订单 |
| 优先级 | P1 |

**测试步骤**：
1. 点击「今天」按钮
2. 观察接口与列表
3. 切换到「本周」
4. 再次点击「今天」取消（互斥单选可清除）

**期望结果**：
- ✅ 接口：`GET /api/orders?dateFrom=YYYY-MM-DD&dateTo=YYYY-MM-DD&...` 200，参数值为今天 0 点至次日 0 点
- ✅ 界面：表格仅显示今日订单
- ✅ 界面：「今天」与「本周」互斥，再次点击当前选项可清除筛选

**通过 / 失败**：☐ / ☐  执行人：____  执行时间：____  Bug 编号（如有）：____

---

### TC-A-021 [P1] 搜索框防抖 500ms 后请求

| 字段 | 内容 |
|---|---|
| 模块 | 订单管理 |
| 前置条件 | 已登录 |
| 测试数据 | 输入「张三」 |
| 优先级 | P1 |

**测试步骤**：
1. 在搜索框快速输入「张」「张三」
2. 用 Network 面板观察请求节流

**期望结果**：
- ✅ 接口：停止输入约 500ms 后才发起 `GET /api/orders?keyword=张三&...`
- ✅ 界面：表格匹配「张三」的订单

**通过 / 失败**：☐ / ☐  执行人：____  执行时间：____  Bug 编号（如有）：____

---

### TC-A-022 [P1] 重置按钮清空所有筛选

| 字段 | 内容 |
|---|---|
| 模块 | 订单管理 |
| 前置条件 | 已登录；已设置状态/日期/搜索三项筛选 |
| 测试数据 | 状态「待接单」+ 日期「今天」+ 关键词「张三」 |
| 优先级 | P1 |

**测试步骤**：
1. 先组合 3 项筛选
2. 点「重置」按钮
3. 观察筛选区和表格

**期望结果**：
- ✅ 界面：所有筛选控件回归默认值
- ✅ 接口：`GET /api/orders?page=1&size=50` 200
- ✅ 界面：表格显示所有订单

**通过 / 失败**：☐ / ☐  执行人：____  执行时间：____  Bug 编号（如有）：____

---

### TC-A-023 [P1] 筛选无结果时显示空状态及清除筛选按钮

| 字段 | 内容 |
|---|---|
| 模块 | 订单管理 |
| 前置条件 | 已登录 |
| 测试数据 | 搜索关键字「不存在的关键字xyz123」 |
| 优先级 | P1 |

**测试步骤**：
1. 搜索框输入「不存在的关键字xyz123」
2. 观察空状态
3. 点击空状态内的「清除筛选」按钮

**期望结果**：
- ✅ 界面：表格区域显示「没有找到订单，试试清除筛选条件」+ 清除筛选按钮
- ✅ 界面：步骤 3 点击后筛选条件清空，表格恢复

**通过 / 失败**：☐ / ☐  执行人：____  执行时间：____  Bug 编号（如有）：____

---

### TC-A-024 [P1] 创建订单时手机号格式与必填校验

| 字段 | 内容 |
|---|---|
| 模块 | 订单管理 |
| 前置条件 | 已登录 |
| 测试数据 | 姓名「测试」，手机号留空 / 「123」 |
| 优先级 | P1 |

**测试步骤**：
1. 创建订单步骤 1，点「新增客户」
2. 姓名留空提交 → 观察错误
3. 姓名填「测试」，手机号「123」（非 11 位）→ 提交
4. 手机号改为 `13800000000`（与张三重复）→ 提交

**期望结果**：
- ✅ 界面：步骤 2 内联错误「姓名必填」
- ✅ 界面：步骤 3 内联错误「手机号需为 11 位」
- ✅ 接口：步骤 4 `POST /api/customers` 返回 422，错误码 `DUPLICATE_PHONE`，吐司「该手机号已存在」

**通过 / 失败**：☐ / ☐  执行人：____  执行时间：____  Bug 编号（如有）：____

---

### TC-A-025 [P2] 订单表格状态徽章颜色对应正确

| 字段 | 内容 |
|---|---|
| 模块 | 订单管理 |
| 前置条件 | 已登录；订单含 5 种状态各 1 笔 |
| 测试数据 | seed 数据 + 手动制造 |
| 优先级 | P2 |

**测试步骤**：
1. 进入订单列表
2. 检查 5 种状态徽章颜色

**期望结果**：
- ✅ 界面：pending=黄、confirmed=蓝、ready=绿、completed=灰、cancelled=红
- ✅ 文案：依次「待接单」「制作中」「待出餐」「已完成」「已取消」

**通过 / 失败**：☐ / ☐  执行人：____  执行时间：____  Bug 编号（如有）：____

---

### TC-A-026 [P2] 抽屉宽度 560px 与时间格式

| 字段 | 内容 |
|---|---|
| 模块 | 订单管理 |
| 前置条件 | 已登录；至少 1 笔订单 |
| 测试数据 | 任一订单 |
| 优先级 | P2 |

**测试步骤**：
1. 打开任一订单详情抽屉
2. DevTools 检查抽屉宽度
3. 检查创建时间显示格式

**期望结果**：
- ✅ 界面：抽屉宽度恰好 560px
- ✅ 界面：创建时间格式 `YYYY-MM-DD HH:mm`
- ✅ 界面：金额格式 `¥XX.XX`

**通过 / 失败**：☐ / ☐  执行人：____  执行时间：____  Bug 编号（如有）：____

---

## 4. CRM (/crm)

### TC-A-027 [P0] 新增客户后列表刷新且手机号唯一性生效

| 字段 | 内容 |
|---|---|
| 模块 | CRM |
| 前置条件 | 已登录 |
| 测试数据 | 姓名「测试王五」、手机号 `13700000001`（事先未占用） |
| 优先级 | P0 |

**测试步骤**：
1. 进入 `/crm`，点「新增客户」
2. 填入姓名「测试王五」、手机号 `13700000001`，点「确定」
3. 观察吐司与列表
4. 再次点「新增客户」，输入相同手机号 `13700000001`
5. 点击「确定」

**期望结果**：
- ✅ 界面：步骤 3 吐司「客户添加成功」，模态框关闭，列表新增一行
- ✅ 接口：步骤 3 `POST /api/customers` 返回 201
- ✅ 数据：customers 表新增一行
- ✅ 界面：步骤 5 吐司「该手机号已存在」，模态框保持打开
- ✅ 接口：步骤 5 `POST /api/customers` 返回 422，错误码 `DUPLICATE_PHONE`

**通过 / 失败**：☐ / ☐  执行人：____  执行时间：____  Bug 编号（如有）：____

---

### TC-A-028 [P0] 编辑客户信息成功保存

| 字段 | 内容 |
|---|---|
| 模块 | CRM |
| 前置条件 | 已登录；存在客户「张三」 |
| 测试数据 | 张三 → 改名为「张三丰」 |
| 优先级 | P0 |

**测试步骤**：
1. 列表中点击「张三」行的姓名
2. 模态框中确认预填了张三的姓名和手机号
3. 修改姓名为「张三丰」，点「确定」
4. 观察列表

**期望结果**：
- ✅ 界面：模态框预填正确
- ✅ 界面：吐司「客户信息已更新」，模态框关闭
- ✅ 接口：`PATCH /api/customers/:id` 200
- ✅ 数据：DB 中 name 已更新为「张三丰」
- ✅ 界面：列表对应行姓名刷新

**通过 / 失败**：☐ / ☐  执行人：____  执行时间：____  Bug 编号（如有）：____

---

### TC-A-029 [P1] 搜索框实时搜索（防抖 500ms）

| 字段 | 内容 |
|---|---|
| 模块 | CRM |
| 前置条件 | 已登录 |
| 测试数据 | 输入「张三」 |
| 优先级 | P1 |

**测试步骤**：
1. 在搜索框快速输入「张」「张三」
2. Network 面板观察请求

**期望结果**：
- ✅ 接口：停止输入约 500ms 后发起 `GET /api/customers?keyword=张三&page=1&size=50` 200
- ✅ 界面：表格显示匹配项

**通过 / 失败**：☐ / ☐  执行人：____  执行时间：____  Bug 编号（如有）：____

---

### TC-A-030 [P1] 搜索手机号也能命中

| 字段 | 内容 |
|---|---|
| 模块 | CRM |
| 前置条件 | 已登录；存在客户手机号 `13800138001` |
| 测试数据 | 输入「13800138001」 |
| 优先级 | P1 |

**测试步骤**：
1. 搜索框输入 `13800138001`
2. 观察结果

**期望结果**：
- ✅ 界面：表格匹配对应客户
- ✅ 接口：`GET /api/customers?keyword=13800138001` 200

**通过 / 失败**：☐ / ☐  执行人：____  执行时间：____  Bug 编号（如有）：____

---

### TC-A-031 [P1] 新增客户表单必填校验

| 字段 | 内容 |
|---|---|
| 模块 | CRM |
| 前置条件 | 已登录 |
| 测试数据 | 姓名留空 / 手机号留空 / 手机号格式错误 |
| 优先级 | P1 |

**测试步骤**：
1. 点「新增客户」，姓名手机号都留空 → 点「确定」
2. 仅填姓名 → 点「确定」
3. 姓名 + 手机号「abc123」 → 点「确定」

**期望结果**：
- ✅ 界面：步骤 1、2 内联错误「姓名必填」/「手机号必填」
- ✅ 界面：步骤 3 内联错误「手机号格式错误」或「请输入 11 位手机号」
- ✅ 接口：均未发起请求

**通过 / 失败**：☐ / ☐  执行人：____  执行时间：____  Bug 编号（如有）：____

---

### TC-A-032 [P1] 姓名超过 20 字符被截断或阻止

| 字段 | 内容 |
|---|---|
| 模块 | CRM |
| 前置条件 | 已登录 |
| 测试数据 | 21 字符姓名「a」x 21 |
| 优先级 | P1 |

**测试步骤**：
1. 新增客户表单，姓名粘贴 21 个 `a`
2. 观察输入框

**期望结果**：
- ✅ 界面：输入框 maxLength=20，最多保留 20 个字符，或显示「最多 20 字符」提示

**通过 / 失败**：☐ / ☐  执行人：____  执行时间：____  Bug 编号（如有）：____

---

### TC-A-033 [P1] 模态框取消按钮关闭且不保存

| 字段 | 内容 |
|---|---|
| 模块 | CRM |
| 前置条件 | 已登录 |
| 测试数据 | 任意输入 |
| 优先级 | P1 |

**测试步骤**：
1. 点「新增客户」，随便填一些内容
2. 点「取消」按钮
3. 再次点「新增客户」

**期望结果**：
- ✅ 界面：步骤 2 模态框关闭，列表无新增
- ✅ 界面：步骤 3 表单为清空状态（不残留上次输入）
- ✅ 数据：customers 表无新增

**通过 / 失败**：☐ / ☐  执行人：____  执行时间：____  Bug 编号（如有）：____

---

### TC-A-034 [P2] 客户列表为空时显示空态

| 字段 | 内容 |
|---|---|
| 模块 | CRM |
| 前置条件 | 已登录；清空 customers 表（仅测试环境） |
| 测试数据 | 无 |
| 优先级 | P2 |

**测试步骤**：
1. 清空客户表后刷新 `/crm`
2. 观察空状态
3. 点击空状态中的「新增客户」按钮

**期望结果**：
- ✅ 界面：表格区域显示「暂无客户，点击新增客户」+ 新增客户按钮
- ✅ 界面：步骤 3 打开新增模态框

**通过 / 失败**：☐ / ☐  执行人：____  执行时间：____  Bug 编号（如有）：____

---

### TC-A-035 [P2] 列表展示客户的累计消费与最后下单时间格式

| 字段 | 内容 |
|---|---|
| 模块 | CRM |
| 前置条件 | 已登录；存在有消费记录的客户 |
| 测试数据 | 张三（有 ≥ 1 笔已完成订单） |
| 优先级 | P2 |

**测试步骤**：
1. 进入 `/crm`
2. 检查张三所在行的「累计消费」「最后下单时间」「订单总数」列

**期望结果**：
- ✅ 界面：累计消费格式 `¥XX,XXX.XX`
- ✅ 界面：最后下单时间格式 `YYYY-MM-DD HH:mm`，无数据时显示 `-`
- ✅ 界面：订单总数为数字（仅展示不可点击）

**通过 / 失败**：☐ / ☐  执行人：____  执行时间：____  Bug 编号（如有）：____

---

## 5. 菜单管理 (/menu)

### TC-A-036 [P0] 新增菜品成功后表格刷新

| 字段 | 内容 |
|---|---|
| 模块 | 菜单管理 |
| 前置条件 | 已登录；选中分类「主食」 |
| 测试数据 | 名称「红烧肉」、分类「主食」、售价 `4800`（=¥48）、库存 50、上架 |
| 优先级 | P0 |

**测试步骤**：
1. 进入 `/menu`，左侧点「主食」分类
2. 右侧点「新增菜品」
3. 模态框中观察分类下拉是否默认选中「主食」
4. 依次填入名称「红烧肉」、售价输入 4800（注意单位是分）、库存 50、上架开启
5. 点「确定」

**期望结果**：
- ✅ 界面：模态框分类预选「主食」
- ✅ 界面：吐司「菜品添加成功」，模态框关闭，表格新增一行
- ✅ 接口：`POST /api/menu-items` 返回 201
- ✅ 数据：menu_items 表新增一行，price=4800，stock=50，available=true
- ✅ 界面：售价显示 `¥48.00`

**通过 / 失败**：☐ / ☐  执行人：____  执行时间：____  Bug 编号（如有）：____

---

### TC-A-037 [P0] 删除被未完成订单引用的菜品被拒

| 字段 | 内容 |
|---|---|
| 模块 | 菜单管理 |
| 前置条件 | 已登录；先创建一笔 pending 订单，含菜品「宫保鸡丁」 |
| 测试数据 | 「宫保鸡丁」 |
| 优先级 | P0 |

**测试步骤**：
1. 先在 `/orders` 创建 pending 订单含宫保鸡丁
2. 切换到 `/menu`，选「主食」
3. 在宫保鸡丁行点「删除」
4. 弹窗点「确定删除」
5. 观察吐司

**期望结果**：
- ✅ 界面：弹窗文案「确定删除该菜品吗？删除后不可恢复。」
- ✅ 接口：`DELETE /api/menu-items/:id` 返回 422 / 400
- ✅ 界面：吐司「该菜品存在未完成订单，无法删除」
- ✅ 数据：menu_items 表对应行未被删除

**通过 / 失败**：☐ / ☐  执行人：____  执行时间：____  Bug 编号（如有）：____

---

### TC-A-038 [P0] 删除菜品成功（无关联订单）

| 字段 | 内容 |
|---|---|
| 模块 | 菜单管理 |
| 前置条件 | 已登录；先新建一个无引用的临时菜品「测试待删」 |
| 测试数据 | 临时菜品 |
| 优先级 | P0 |

**测试步骤**：
1. 在「主食」分类新建菜品「测试待删」
2. 在表格中点「删除」
3. 弹窗点「确定删除」

**期望结果**：
- ✅ 界面：吐司「菜品已删除」，表格中行消失
- ✅ 接口：`DELETE /api/menu-items/:id` 200
- ✅ 数据：menu_items 表对应行已删除

**通过 / 失败**：☐ / ☐  执行人：____  执行时间：____  Bug 编号（如有）：____

---

### TC-A-039 [P0] 删除有菜品的分类被拒

| 字段 | 内容 |
|---|---|
| 模块 | 菜单管理 |
| 前置条件 | 已登录；「主食」分类下有 ≥ 1 个菜品 |
| 测试数据 | 「主食」分类 |
| 优先级 | P0 |

**测试步骤**：
1. 悬浮在左侧「主食」分类上，点删除按钮
2. 弹窗点「确定删除」

**期望结果**：
- ✅ 界面：弹窗文案「确定删除该分类吗？」
- ✅ 接口：`DELETE /api/categories/:id` 返回 422 / 400
- ✅ 界面：吐司「该分类下存在菜品，无法删除」
- ✅ 数据：categories 表对应行未被删除

**通过 / 失败**：☐ / ☐  执行人：____  执行时间：____  Bug 编号（如有）：____

---

### TC-A-040 [P0] 上架/下架开关切换且非乐观更新

| 字段 | 内容 |
|---|---|
| 模块 | 菜单管理 |
| 前置条件 | 已登录；存在上架菜品「宫保鸡丁」 |
| 测试数据 | 宫保鸡丁 |
| 优先级 | P0 |

**测试步骤**：
1. 在表格中找到「宫保鸡丁」，点击上架开关
2. 观察开关动画：是否先 loading 再变更
3. 等待请求返回，观察吐司
4. 再次点击开关切换回去

**期望结果**：
- ✅ 界面：点击后开关立即 loading（菊花/禁用），UI 状态不立刻翻转
- ✅ 接口：`PATCH /api/menu-items/:id` 仅传 `{ available: false }` 200
- ✅ 界面：成功后 UI 切换为下架态，吐司「菜品已下架」
- ✅ 数据：DB 中 available=false
- ✅ 界面：步骤 4 切回上架，吐司「菜品已上架」

**通过 / 失败**：☐ / ☐  执行人：____  执行时间：____  Bug 编号（如有）：____

---

### TC-A-041 [P1] 新增分类成功并出现在左侧列表

| 字段 | 内容 |
|---|---|
| 模块 | 菜单管理 |
| 前置条件 | 已登录 |
| 测试数据 | 名称「凉菜」、排序权重 5 |
| 优先级 | P1 |

**测试步骤**：
1. 点左侧「+ 新增分类」
2. 输入名称「凉菜」、排序权重 5
3. 点「确定」

**期望结果**：
- ✅ 界面：吐司「分类添加成功」，模态框关闭
- ✅ 界面：左侧分类列表出现「凉菜」，按 sortOrder 升序排列
- ✅ 接口：`POST /api/categories` 201
- ✅ 数据：categories 表新增一行

**通过 / 失败**：☐ / ☐  执行人：____  执行时间：____  Bug 编号（如有）：____

---

### TC-A-042 [P1] 编辑分类并修改排序权重

| 字段 | 内容 |
|---|---|
| 模块 | 菜单管理 |
| 前置条件 | 已登录；存在分类「凉菜」 |
| 测试数据 | 凉菜 → 排序改为 0 |
| 优先级 | P1 |

**测试步骤**：
1. 悬浮「凉菜」分类，点编辑按钮
2. 修改排序权重为 0，点「确定」
3. 观察分类列表顺序

**期望结果**：
- ✅ 界面：吐司「分类已更新」
- ✅ 界面：「凉菜」在列表中按新排序权重移动位置
- ✅ 接口：`PATCH /api/categories/:id` 200

**通过 / 失败**：☐ / ☐  执行人：____  执行时间：____  Bug 编号（如有）：____

---

### TC-A-043 [P1] 切换分类时右侧菜品表格刷新

| 字段 | 内容 |
|---|---|
| 模块 | 菜单管理 |
| 前置条件 | 已登录；至少 2 个分类各有菜品 |
| 测试数据 | 主食 / 饮品 |
| 优先级 | P1 |

**测试步骤**：
1. 点左侧「主食」，观察表格
2. 点左侧「饮品」，观察表格
3. 检查面包屑变化

**期望结果**：
- ✅ 接口：每次切换发起 `GET /api/menu-items?categoryId=xxx&page=1&size=50` 200
- ✅ 界面：表格内容刷新，仅显示当前分类的菜品
- ✅ 界面：面包屑显示当前分类名
- ✅ 界面：选中分类有高亮背景

**通过 / 失败**：☐ / ☐  执行人：____  执行时间：____  Bug 编号（如有）：____

---

### TC-A-044 [P1] 编辑菜品表单字段校验（名称必填、售价正整数、库存范围）

| 字段 | 内容 |
|---|---|
| 模块 | 菜单管理 |
| 前置条件 | 已登录 |
| 测试数据 | 名称空 / 售价 0 / 售价 -1 / 库存 -2 |
| 优先级 | P1 |

**测试步骤**：
1. 点「新增菜品」
2. 名称留空 → 提交
3. 名称填「test」，售价输 0 → 提交
4. 售价输 -1 → 提交
5. 售价输 1000，库存输 -2 → 提交
6. 库存输 -1（表示不限） → 提交

**期望结果**：
- ✅ 界面：步骤 2 内联「菜品名称必填」
- ✅ 界面：步骤 3、4 内联「售价必须为正整数（分）」
- ✅ 界面：步骤 5 内联「库存需为非负整数或 -1（不限）」
- ✅ 界面：步骤 6 校验通过，提交成功（库存显示为「不限」）

**通过 / 失败**：☐ / ☐  执行人：____  执行时间：____  Bug 编号（如有）：____

---

### TC-A-045 [P1] 菜品名称超 30 字符 / 描述超 200 字符

| 字段 | 内容 |
|---|---|
| 模块 | 菜单管理 |
| 前置条件 | 已登录 |
| 测试数据 | 31 字符名称 / 201 字符描述 |
| 优先级 | P1 |

**测试步骤**：
1. 新增菜品，名称粘贴 31 个 `a`
2. 描述粘贴 201 个 `b`
3. 提交

**期望结果**：
- ✅ 界面：名称输入框最多保留 30 字符
- ✅ 界面：描述输入框最多保留 200 字符或提示「最多 200 字符」

**通过 / 失败**：☐ / ☐  执行人：____  执行时间：____  Bug 编号（如有）：____

---

### TC-A-046 [P1] 库存为 0 时仍可上架但不可被下单（场景验证）

| 字段 | 内容 |
|---|---|
| 模块 | 菜单管理 |
| 前置条件 | 已登录；创建菜品「测试零库存」库存=0、上架 |
| 测试数据 | 测试零库存 |
| 优先级 | P1 |

**测试步骤**：
1. 在 `/menu` 创建菜品 stock=0、available=true
2. 切到 `/orders` 创建订单包含此菜品
3. 观察接口响应

**期望结果**：
- ✅ 界面：菜单列表中正常显示且开关为上架
- ✅ 接口：创建订单时 `POST /api/orders` 返回 422，错误码 `INSUFFICIENT_STOCK`
- ✅ 界面：订单页吐司「部分菜品已下架或库存不足」

**通过 / 失败**：☐ / ☐  执行人：____  执行时间：____  Bug 编号（如有）：____

---

### TC-A-047 [P2] 空分类时右侧显示空状态

| 字段 | 内容 |
|---|---|
| 模块 | 菜单管理 |
| 前置条件 | 已登录；新建一个空分类「凉菜」 |
| 测试数据 | 空分类 |
| 优先级 | P2 |

**测试步骤**：
1. 新建分类「凉菜」
2. 点击「凉菜」分类

**期望结果**：
- ✅ 界面：右侧表格区显示「此分类下暂无菜品，点击新增菜品」+ 新增菜品按钮
- ✅ 界面：分类项右侧角标为 0

**通过 / 失败**：☐ / ☐  执行人：____  执行时间：____  Bug 编号（如有）：____

---

### TC-A-048 [P2] 菜品图片 URL 预览与无图占位

| 字段 | 内容 |
|---|---|
| 模块 | 菜单管理 |
| 前置条件 | 已登录 |
| 测试数据 | 图片 URL `https://placehold.co/64x64` / 留空 |
| 优先级 | P2 |

**测试步骤**：
1. 新增菜品，图片 URL 填 `https://placehold.co/64x64`，提交
2. 在表格中观察缩略图
3. 再新增一个图片 URL 留空的菜品
4. 在表格中观察其图片列

**期望结果**：
- ✅ 界面：步骤 2 显示 64×64 的图片
- ✅ 界面：步骤 4 显示占位图
- ✅ 界面：模态框中填入 URL 时可即时预览

**通过 / 失败**：☐ / ☐  执行人：____  执行时间：____  Bug 编号（如有）：____

---

## 6. 设置 (/settings)

### TC-A-049 [P0] 加载设置并保存修改

| 字段 | 内容 |
|---|---|
| 模块 | 设置 |
| 前置条件 | 已登录 |
| 测试数据 | 准备时间 25、自动接单 ON、营业开启、营业时间 09:00 ~ 22:00 |
| 优先级 | P0 |

**测试步骤**：
1. 进入 `/settings`，观察各字段值
2. 修改准备时间为 25
3. 切换自动接单为开
4. 营业时间结束改为 23:00
5. 点「保存」

**期望结果**：
- ✅ 接口：进入时 `GET /api/settings` 200，表单预填
- ✅ 界面：保存按钮 loading
- ✅ 接口：`PUT /api/settings` 200，请求体含全部字段
- ✅ 界面：吐司「设置已保存」
- ✅ 数据：DB 中 settings 表的对应字段更新

**通过 / 失败**：☐ / ☐  执行人：____  执行时间：____  Bug 编号（如有）：____

---

### TC-A-050 [P0] 准备时间边界值校验（1、180、0、181）

| 字段 | 内容 |
|---|---|
| 模块 | 设置 |
| 前置条件 | 已登录 |
| 测试数据 | 0 / 1 / 180 / 181 |
| 优先级 | P0 |

**测试步骤**：
1. 准备时间输入 0，点「保存」
2. 输入 1，点「保存」
3. 输入 180，点「保存」
4. 输入 181，点「保存」

**期望结果**：
- ✅ 界面：步骤 1 内联「准备时间需在 1~180 分钟之间」，未发请求
- ✅ 界面：步骤 2、3 保存成功，吐司「设置已保存」
- ✅ 界面：步骤 4 内联「准备时间需在 1~180 分钟之间」，未发请求

**通过 / 失败**：☐ / ☐  执行人：____  执行时间：____  Bug 编号（如有）：____

---

### TC-A-051 [P0] 营业时间结束必须大于开始时间

| 字段 | 内容 |
|---|---|
| 模块 | 设置 |
| 前置条件 | 已登录 |
| 测试数据 | 开始 22:00、结束 09:00 |
| 优先级 | P0 |

**测试步骤**：
1. 开始时间设 22:00，结束时间设 09:00
2. 点「保存」

**期望结果**：
- ✅ 界面：内联「结束时间必须大于开始时间」
- ✅ 接口：未发起 PUT 请求

**通过 / 失败**：☐ / ☐  执行人：____  执行时间：____  Bug 编号（如有）：____

---

### TC-A-052 [P0] 自动接单开关影响下单后初始状态

| 字段 | 内容 |
|---|---|
| 模块 | 设置 |
| 前置条件 | 已登录 |
| 测试数据 | 自动接单 OFF → 创建订单 → 自动接单 ON → 再创建订单 |
| 优先级 | P0 |

**测试步骤**：
1. 设置自动接单为 OFF，保存
2. 在 `/orders` 创建一笔订单
3. 观察新订单状态
4. 回到 `/settings` 设自动接单 ON，保存
5. 在 `/orders` 再创建一笔订单
6. 观察新订单状态

**期望结果**：
- ✅ 界面：步骤 3 订单状态为「待接单（pending）」
- ✅ 界面：步骤 6 订单状态为「制作中（confirmed）」
- ✅ 数据：服务端按 settings.autoAccept 决定初始状态

**通过 / 失败**：☐ / ☐  执行人：____  执行时间：____  Bug 编号（如有）：____

---

### TC-A-053 [P1] 营业状态开关切换文字「营业中 / 已打烊」

| 字段 | 内容 |
|---|---|
| 模块 | 设置 |
| 前置条件 | 已登录 |
| 测试数据 | 营业状态 ON / OFF |
| 优先级 | P1 |

**测试步骤**：
1. 进入设置页
2. 营业状态当前为 ON，观察显示文字
3. 点击开关切到 OFF
4. 观察文字
5. 不点保存，刷新页面

**期望结果**：
- ✅ 界面：步骤 2 显示「营业中」
- ✅ 界面：步骤 4 显示「已打烊」
- ✅ 界面：步骤 5 刷新后状态回到上次已保存值（未点保存的修改丢弃）

**通过 / 失败**：☐ / ☐  执行人：____  执行时间：____  Bug 编号（如有）：____

---

### TC-A-054 [P1] 后端 5xx 错误时保存按钮恢复并吐司

| 字段 | 内容 |
|---|---|
| 模块 | 设置 |
| 前置条件 | 已登录；DevTools 可拦截 |
| 测试数据 | 拦截 `PUT /api/settings` 返回 500 |
| 优先级 | P1 |

**测试步骤**：
1. DevTools 拦截 `PUT /api/settings` → 模拟返回 500
2. 修改任一字段
3. 点「保存」

**期望结果**：
- ✅ 界面：保存按钮先 loading 然后恢复可点
- ✅ 界面：吐司红色错误信息

**通过 / 失败**：☐ / ☐  执行人：____  执行时间：____  Bug 编号（如有）：____

---

### TC-A-055 [P1] 保存按钮 loading 时禁用防重复提交

| 字段 | 内容 |
|---|---|
| 模块 | 设置 |
| 前置条件 | 已登录；DevTools 可减慢网络 |
| 测试数据 | Network 限速 Slow 3G |
| 优先级 | P1 |

**测试步骤**：
1. DevTools Network 限速 Slow 3G
2. 修改字段后连续快速点击「保存」3 次

**期望结果**：
- ✅ 界面：第一次点击后按钮 loading 禁用，后续点击不触发新请求
- ✅ 接口：仅发起 1 次 `PUT /api/settings`

**通过 / 失败**：☐ / ☐  执行人：____  执行时间：____  Bug 编号（如有）：____

---

### TC-A-056 [P2] 准备时间输入非数字被禁止

| 字段 | 内容 |
|---|---|
| 模块 | 设置 |
| 前置条件 | 已登录 |
| 测试数据 | 输入 abc / 15.5 / 中文 |
| 优先级 | P2 |

**测试步骤**：
1. 在准备时间输入框输入「abc」
2. 输入「15.5」
3. 输入中文「十」

**期望结果**：
- ✅ 界面：数字输入框拒绝非数字字符
- ✅ 界面：小数被禁止或舍入为整数
- ✅ 界面：内联错误「请输入整数」

**通过 / 失败**：☐ / ☐  执行人：____  执行时间：____  Bug 编号（如有）：____

---

### TC-A-057 [P2] 设置页区块标题与表单间距使用设计令牌

| 字段 | 内容 |
|---|---|
| 模块 | 设置 |
| 前置条件 | 已登录 |
| 测试数据 | 无 |
| 优先级 | P2 |

**测试步骤**：
1. 进入设置页
2. DevTools 检查页面标题「门店设置」字号、区块标题「出餐设置」「营业状态」字号
3. 检查区块间距

**期望结果**：
- ✅ 界面：页面标题使用 heading-1（28px / 600）
- ✅ 界面：区块标题使用 heading-2（20px / 600）
- ✅ 界面：区块间距 32px（spacing-xl）

**通过 / 失败**：☐ / ☐  执行人：____  执行时间：____  Bug 编号（如有）：____

---

## 7. UI 组件库 (/ui-library)

### TC-A-058 [P1] 颜色令牌色块色值与设计系统一致

| 字段 | 内容 |
|---|---|
| 模块 | UI 组件库 |
| 前置条件 | 浏览器直接访问 `/ui-library`（无需登录） |
| 测试数据 | 颜色令牌（详见总览 §2 ai需求.md 第 2.1 节） |
| 优先级 | P1 |

**测试步骤**：
1. 访问 `/ui-library`
2. 滚动到「颜色令牌」区块
3. DevTools 选中 `color-primary` 色块，检查 background-color
4. 同样检查 success / warning / error 色块

**期望结果**：
- ✅ 界面：color-primary = #F59E0B
- ✅ 界面：color-success = #10B981、warning = #F97316、error = #EF4444
- ✅ 界面：每个色块尺寸约 120×60px，下方标注令牌名 + 色值

**通过 / 失败**：☐ / ☐  执行人：____  执行时间：____  Bug 编号（如有）：____

---

### TC-A-059 [P1] 字体排印各样式字号 / 行高 / 字重正确

| 字段 | 内容 |
|---|---|
| 模块 | UI 组件库 |
| 前置条件 | 在 `/ui-library` |
| 测试数据 | heading-1 / heading-2 / body-base / caption |
| 优先级 | P1 |

**测试步骤**：
1. 滚动到「字体排印」区块
2. DevTools 检查各示例的 font-size / line-height / font-weight

**期望结果**：
- ✅ heading-1：28px / 36px / 600
- ✅ heading-2：20px / 28px / 600
- ✅ body-base：14px / 20px / 400
- ✅ caption：12px / 16px / 400

**通过 / 失败**：☐ / ☐  执行人：____  执行时间：____  Bug 编号（如有）：____

---

### TC-A-060 [P1] 模态框演示点击遮罩与 ESC 可关闭

| 字段 | 内容 |
|---|---|
| 模块 | UI 组件库 |
| 前置条件 | 在 `/ui-library` |
| 测试数据 | 无 |
| 优先级 | P1 |

**测试步骤**：
1. 滚动到「模态框」组件示例
2. 点「打开模态框」按钮
3. 点击模态框外侧遮罩 → 是否关闭
4. 再次打开，按 ESC 键 → 是否关闭

**期望结果**：
- ✅ 界面：模态框居中弹出
- ✅ 界面：步骤 3、4 均能关闭模态框
- ✅ 界面：模态框圆角 12px（radius-lg）

**通过 / 失败**：☐ / ☐  执行人：____  执行时间：____  Bug 编号（如有）：____

---

### TC-A-061 [P1] 吐司演示触发 3 种状态吐司且 4 秒消失

| 字段 | 内容 |
|---|---|
| 模块 | UI 组件库 |
| 前置条件 | 在 `/ui-library` |
| 测试数据 | 成功 / 错误 / 警告 |
| 优先级 | P1 |

**测试步骤**：
1. 滚动到反馈组件区
2. 依次点击 3 个吐司触发按钮
3. 观察位置与停留时间

**期望结果**：
- ✅ 界面：吐司从右上角弹出
- ✅ 界面：成功绿、错误红、警告橙
- ✅ 界面：4 秒后自动消失
- ✅ 界面：同时点击多个最多堆叠 3 条

**通过 / 失败**：☐ / ☐  执行人：____  执行时间：____  Bug 编号（如有）：____

---

### TC-A-062 [P2] 左侧导航锚点点击滚动到对应区块

| 字段 | 内容 |
|---|---|
| 模块 | UI 组件库 |
| 前置条件 | 在 `/ui-library` |
| 测试数据 | 6 个锚点 |
| 优先级 | P2 |

**测试步骤**：
1. 依次点击左侧「颜色令牌」「字体排印」「间距刻度」「表面样式」「组件示例」「状态模式」
2. 观察页面滚动

**期望结果**：
- ✅ 界面：每次点击滚动到对应区块
- ✅ 界面：当前可视区块对应的导航项高亮

**通过 / 失败**：☐ / ☐  执行人：____  执行时间：____  Bug 编号（如有）：____

---

### TC-A-063 [P2] 按钮组件所有状态视觉对照（hover / focus / disabled）

| 字段 | 内容 |
|---|---|
| 模块 | UI 组件库 |
| 前置条件 | 在 `/ui-library` |
| 测试数据 | 主要按钮、次要按钮、文本按钮、危险按钮 |
| 优先级 | P2 |

**测试步骤**：
1. 滚动到按钮组件区
2. 鼠标悬浮主要按钮 → 检查 hover 色 #D97706
3. 切换到 Tab 焦点
4. 观察 disabled 按钮的禁用样式

**期望结果**：
- ✅ 界面：主要按钮 hover 色 #D97706
- ✅ 界面：focus 态有边框 1px solid #F59E0B
- ✅ 界面：disabled 态背景 #F3F4F6，鼠标 not-allowed
- ✅ 界面：危险按钮使用 color-error #EF4444

**通过 / 失败**：☐ / ☐  执行人：____  执行时间：____  Bug 编号（如有）：____

---

### TC-A-064 [P2] 间距刻度色条宽度与令牌值一致

| 字段 | 内容 |
|---|---|
| 模块 | UI 组件库 |
| 前置条件 | 在 `/ui-library` |
| 测试数据 | xs/sm/md/lg/xl/2xl |
| 优先级 | P2 |

**测试步骤**：
1. 滚动到「间距刻度」区块
2. DevTools 测量每个色条宽度

**期望结果**：
- ✅ 界面：色条宽度依次 4 / 8 / 16 / 24 / 32 / 48 px
- ✅ 界面：色条高度 40px

**通过 / 失败**：☐ / ☐  执行人：____  执行时间：____  Bug 编号（如有）：____

---

### TC-A-065 [P2] 徽章组件 5 种状态颜色

| 字段 | 内容 |
|---|---|
| 模块 | UI 组件库 |
| 前置条件 | 在 `/ui-library` |
| 测试数据 | 进行中 / 成功 / 警告 / 错误 / 禁用 |
| 优先级 | P2 |

**测试步骤**：
1. 滚动到徽章组件示例
2. 检查 5 个徽章的颜色与圆角

**期望结果**：
- ✅ 界面：徽章圆角使用 radius-full（9999px）
- ✅ 界面：颜色分别对应蓝 / 绿 / 橙 / 红 / 灰系

**通过 / 失败**：☐ / ☐  执行人：____  执行时间：____  Bug 编号（如有）：____

---

## 8. 错误兜底与全局行为

### TC-A-066 [P0] 接口 401（token 过期）触发跳转登录

| 字段 | 内容 |
|---|---|
| 模块 | 错误兜底 |
| 前置条件 | 已登录；DevTools 修改 localStorage token 为 `invalid_token` |
| 测试数据 | 无效 token |
| 优先级 | P0 |

**测试步骤**：
1. 登录后 DevTools 改写 token
2. 刷新页面或访问任一受保护路由
3. 观察跳转

**期望结果**：
- ✅ 接口：受保护接口返回 401
- ✅ 界面：前端拦截 401 后清除 token 并跳转至 `/login`
- ✅ 界面：吐司「登录已过期，请重新登录」或类似提示

**通过 / 失败**：☐ / ☐  执行人：____  执行时间：____  Bug 编号（如有）：____

---

### TC-A-067 [P1] 接口 500 错误时显示友好吐司

| 字段 | 内容 |
|---|---|
| 模块 | 错误兜底 |
| 前置条件 | 已登录；可拦截接口 |
| 测试数据 | 拦截 `GET /api/orders` 返回 500 |
| 优先级 | P1 |

**测试步骤**：
1. DevTools 拦截 `GET /api/orders` 返回 500
2. 进入 `/orders`
3. 观察界面与吐司

**期望结果**：
- ✅ 界面：列表区显示错误兜底「加载失败，请稍后重试」
- ✅ 界面：吐司红色「服务异常，请稍后再试」

**通过 / 失败**：☐ / ☐  执行人：____  执行时间：____  Bug 编号（如有）：____

---

### TC-A-068 [P1] 网络断开离线模式行为

| 字段 | 内容 |
|---|---|
| 模块 | 错误兜底 |
| 前置条件 | 已登录 |
| 测试数据 | DevTools Network 设 Offline |
| 优先级 | P1 |

**测试步骤**：
1. DevTools Network Offline
2. 在 `/orders` 创建订单点提交
3. 观察界面

**期望结果**：
- ✅ 界面：提交按钮恢复可点
- ✅ 界面：吐司「网络异常，请检查网络后重试」
- ✅ 数据：DB 无新订单

**通过 / 失败**：☐ / ☐  执行人：____  执行时间：____  Bug 编号（如有）：____

---

### TC-A-069 [P2] 多吐司同时弹出最多堆叠 3 条

| 字段 | 内容 |
|---|---|
| 模块 | 错误兜底 |
| 前置条件 | 在 `/ui-library` |
| 测试数据 | 连续触发 5 个吐司 |
| 优先级 | P2 |

**测试步骤**：
1. 在 UI 库吐司演示区，1 秒内连续点击 5 次成功吐司按钮
2. 观察右上角

**期望结果**：
- ✅ 界面：同时最多显示 3 条吐司，多余的排队或丢弃
- ✅ 界面：每条吐司 4 秒后自动消失

**通过 / 失败**：☐ / ☐  执行人：____  执行时间：____  Bug 编号（如有）：____

---

> 文档结束 · 共 69 条用例 · 编号 TC-A-001 ~ TC-A-069 连续无跳号
